#include "stdio.h"
#include "string.h"
#include "ctype.h"
#include "stdlib.h"

#include "math.h"
#include "time.h"

#define OK 1
#define ERROR 0
#define TRUE 1
#define FALSE 0

#define MAXSIZE 20 /* ????????????·????? */

typedef int Status;   /* Status?????????à??,???????????á??×????ú??????OK?? */
typedef int ElemType; /* ElemType?à???ù???????é?????¨?????????è??int */

Status visit(ElemType c)
{
    printf("%d ", c);
    return OK;
}

typedef struct Node
{
    ElemType data;     // 数据字段，存储数据元素。ElemType 需要事先定义或替换为实际的数据类型，比如 int、char 等。
    struct Node *next; // 指针字段，指向下一个链表节点
} Node;
typedef struct Node *LinkList; /* 定义LinkList */

/* ??????????????±í */
Status InitList(LinkList *L)
{
    *L = (LinkList)malloc(sizeof(Node)); /* ?ú?ú?·?á??,????L???ò???·?á?? */
    if (!(*L))                           /* ????·????§°? */
        return ERROR;
    (*L)->next = NULL; /* ?????ò???? */

    return OK;
}

/* ??????????????????±íL??????????×÷?á??????L????±í???ò·???TRUE??·??ò·???FALSE */
Status ListEmpty(LinkList L)
{
    if (L->next)
        return FALSE;
    else
        return TRUE;
}

/* ??????????????????±íL??????????×÷?á??????L????????±í */
Status ClearList(LinkList *L) // 这不难理解。
{
    LinkList p, q;
    p = (*L)->next; /*  p???ò???????á?? */
    while (p)       /*  ????±í?? */
    {
        q = p->next;
        free(p);
        p = q;
    }
    (*L)->next = NULL; /* ?·?á???????ò???? */
    return OK;
}

/* ??????????????????±íL??????????×÷?á????·???L?????????????? */
int ListLength(LinkList L)
{
    int i = 0;
    LinkList p = L->next; /* p???ò???????á?? */
    while (p)
    {
        i++;
        p = p->next;
    }
    return i;
}

/* ??????????????????±íL????????1??i??ListLength(L) */
/* ??×÷?á??????e·???L????i?????????????? */
Status GetElem(LinkList L, int i, ElemType *e)
{
    int j;
    LinkList p;        /* ?ù?÷???á??p */
    p = L->next;       /* ??p???ò??±íL?????????á?? */
    j = 1;             /*  j???????÷ */
    while (p && j < i) /* p???????ò???????÷j??????????i?±?????·???? */
    {
        p = p->next; /* ??p???ò???????á?? */
        ++j;
    }
    if (!p || j > i)
        return ERROR; /*  ??i???????????? */
    *e = p->data;     /*  ????i???????????? */
    return OK;
}

/* ??????????????????±íL?????? */
/* ??×÷?á????·???L????1????e?ú×????????????????????ò?? */
/* ?????ù???????????????????ò·???????0 */
int LocateElem(LinkList L, ElemType e)
{
    int i = 0;
    LinkList p = L->next;
    while (p)
    {
        i++;
        if (p->data == e) /* ???????ù?????????? */
            return i;
        p = p->next;
    }

    return 0;
}

/* ??????????????????±íL??????,1??i??ListLength(L)?? */
/* ??×÷?á??????L????i?????????°????????????????e??L???¤????1 */
Status ListInsert(LinkList *L, int i, ElemType e)
{
    int j;
    LinkList p, s;
    p = *L;
    j = 1;
    while (p && j < i) /* ?°????i???á?? */
    {
        p = p->next;
        ++j;
    }
    if (!p || j > i)
        return ERROR;                   /* ??i???????????? */
    s = (LinkList)malloc(sizeof(Node)); /*  ?ú?????á??(C????±ê×?????) */
    s->data = e;
    s->next = p->next; /* ??p???ó???á????????s???ó??  */
    p->next = s;       /* ??s??????p???ó?? */
    return OK;
}

/* ??????????????????±íL????????1??i??ListLength(L) */
/* ??×÷?á????????L????i????????????????e·?????????L???¤????1 */
Status ListDelete(LinkList *L, int i, ElemType *e)
{
    int j;
    LinkList p, q;
    p = *L;
    j = 1;
    while (p->next && j < i) /* ±é?ú?°????i?????? */
    {
        p = p->next;
        ++j;
    }
    if (!(p->next) || j > i)
        return ERROR; /* ??i???????????? */
    q = p->next;
    p->next = q->next; /* ??q???ó????????p???ó?? */
    *e = q->data;      /* ??q?á????????????e */
    free(q);           /* ?????????????á??????·????? */
    return OK;
}

/* ??????????????????±íL?????? */
/* ??×÷?á??????????L?????????????????? */
Status ListTraverse(LinkList L)
{
    LinkList p = L->next;
    while (p)
    {
        visit(p->data);
        p = p->next;
    }
    printf("\n");
    return OK;
}

/*  ???ú?ú?ún?????????????¨????±í?·?á????????????±íL?¨?·??·¨?? */
void CreateListHead(LinkList *L, int n)
{
    LinkList p;
    int i;
    srand(time(0));                                      /* ?????????ú????×? */
    *L = (LinkList)malloc(sizeof(Node));                 // 这L是一个指针，用于保存分配的内存的地址。但并不指向链表中任意结点。整个语句的意思是：动态分配一个大小为Node结构体的内存块，并将这块内存的地址转换为LinkList类型的指针，然后赋值给L。
    (*L)->next = NULL; /*  ???¨?????????·?á????????±í */ // 在链表的语境中，将next指针设置为NULL意味着当前结点（由L指针指向）没有指向任何后续结点，即它是链表的最后一个结点。这通常在创建新结点或初始化链表的尾部结点时使用，以确保链表的末尾有一个明确的结束标志。在C语言中，NULL指针常被用作链表、树等数据结构的末尾或结束标志。
    for (i = 0; i < n; i++)
    {
        p = (LinkList)malloc(sizeof(Node)); /*  ?ú?????á?? */
        p->data = rand() % 100 + 1;         /*  ???ú?ú??100????????×? */
        p->next = (*L)->next;               //(*L)->next 是当前链表头部后面的第一个结点。也就是说，它是链表的第二个结点（如果存在的话）。这句代码将新创建的结点 p 的 next 指向当前链表的第二个结点。
        (*L)->next = p; /*  ??????±í?· */   // 这句代码实际上改变了链表头部后面的结点。之前，链表头部后面可能是一个旧的结点或 NULL（如果链表是空的）。但这句代码将其改为新结点 p。结果是，新结点 p 成为了链表的第二个结点，而原来的第二个结点现在成为了第三个结点。
    }                                       // 在这段代码的循环中，新结点总是被插入到链表的头部。因此，最先插入的结点最后会在链表的尾部，而最后插入的结点会在链表的头部。这就是头插法的特点。简单来说，这两行代码共同作用是将新结点插入到链表的头部，但确保它是链表头之后的第一个结点。
}

/*  ???ú?ú?ún?????????????¨????±í?·?á????????????±íL?¨????·¨?? */
void CreateListTail(LinkList *L, int n)
{
    LinkList p, r;
    int i;
    srand(time(0));                      /* ?????????ú????×? */
    *L = (LinkList)malloc(sizeof(Node)); /* L??????????±í */
    r = *L;                              /* r?????ò???????á?? */
    for (i = 0; i < n; i++)
    {
        p = (Node *)malloc(sizeof(Node));                 /*  ?ú?????á?? */
        p->data = rand() % 100 + 1;                       /*  ???ú?ú??100????????×? */
        r->next = p; /* ??±í???????á???????????ò???á?? */ // 在这时，r 是指向链表当前的尾部结点的指针。这句代码将新创建的结点 p 连接到链表的尾部。也就是说，原本链表尾部的 next 指针（它之前可能是 NULL）现在指向了新结点 p。这样，新结点 p 成为了链表的新尾部结点。
        r = p; /* ???±?°?????á???¨????±í???????á?? */     // 这句代码更新了 r 的值，使其指向新的尾部结点 p。这是为了下一次循环时，r 依然指向链表的尾部，因此可以将下一个新结点连接到链表的尾部。
    }                                                     // 这个循环的效果是：对于每次迭代，它都会创建一个新的结点 p，然后将其添加到链表的尾部，随后更新 r 以确保它始终指向链表的尾部。这样，在每次循环结束后，新结点都会被添加到链表的尾部。简而言之，这两行代码确保新结点总是被添加到链表的尾部，并更新 r 的值，使其始终指向链表的最新的尾部结点。
    r->next = NULL;                                       /* ±í???±?°??±í?á?? */
}

int main()
{
    LinkList L;
    ElemType e;
    Status i;
    int j, k;
    i = InitList(&L);
    printf("??????L?ó??ListLength(L)=%d\n", ListLength(L));
    for (j = 1; j <= 5; j++)
        i = ListInsert(&L, 1, j);
    printf("??L??±í?·????????1??5?ó??L.data=");
    ListTraverse(L);

    printf("ListLength(L)=%d \n", ListLength(L));
    i = ListEmpty(L);
    printf("L??·?????i=%d(1:?? 0:·?)\n", i);

    i = ClearList(&L);
    printf("????L?ó??ListLength(L)=%d\n", ListLength(L));
    i = ListEmpty(L);
    printf("L??·?????i=%d(1:?? 0:·?)\n", i);

    for (j = 1; j <= 10; j++)
        ListInsert(&L, j, j);
    printf("??L??±í??????????1??10?ó??L.data=");
    ListTraverse(L);

    printf("ListLength(L)=%d \n", ListLength(L));

    ListInsert(&L, 1, 0);
    printf("??L??±í?·????0?ó??L.data=");
    ListTraverse(L);
    printf("ListLength(L)=%d \n", ListLength(L));

    GetElem(L, 5, &e);
    printf("??5??????????????%d\n", e);
    for (j = 3; j <= 4; j++)
    {
        k = LocateElem(L, j);
        if (k)
            printf("??%d????????????%d\n", k, j);
        else
            printf("????????%d??????\n", j);
    }

    k = ListLength(L); /* k??±í?¤ */
    for (j = k + 1; j >= k; j--)
    {
        i = ListDelete(&L, j, &e); /* ??????j?????? */
        if (i == ERROR)
            printf("??????%d???????§°?\n", j);
        else
            printf("??????%d??????????????%d\n", j, e);
    }
    printf("????????L????????");
    ListTraverse(L);

    j = 5;
    ListDelete(&L, j, &e); /* ??????5?????? */
    printf("??????%d??????????????%d\n", j, e);

    printf("????????L????????");
    ListTraverse(L);

    i = ClearList(&L);
    printf("\n????L?ó??ListLength(L)=%d\n", ListLength(L));
    CreateListHead(&L, 20);
    printf("???????¨L??????(?·??·¨)??");
    ListTraverse(L);

    i = ClearList(&L);
    printf("\n????L?ó??ListLength(L)=%d\n", ListLength(L));
    CreateListTail(&L, 20);
    printf("???????¨L??????(????·¨)??");
    ListTraverse(L);

    return 0;
}
